meta {
  name: PATCH Mark Notification Seen
  type: http
  seq: 2
}

patch {
  url: {{ base_url }}/api/notifications/{{ notif_id }}/seen
  body: none
  auth: inherit
  headers: {
    "Authorization": "Bearer {{ jwt }}"
  }
}
