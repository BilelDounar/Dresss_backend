meta {
  name: POST Like
  type: http
  seq: 1
}

post {
  url: {{ base_url }}/api/likes
  body: json
  auth: inherit
  headers: {
    "Authorization": "Bearer {{ jwt }}",
    "Content-Type": "application/json"
  }
}

body:json {
  {
    "postId": "{{ post_id }}"
  }
}
