module.exports = {
    testEnvironment: 'node',
    verbose: true,
    // Automatically clear mock calls and instances between every test
    clearMocks: true,
    // Look for tests in __tests__ or *.test.js files
    testMatch: ['**/__tests__/**/*.js?(x)', '**/?(*.)+(test).js?(x)'],
};
