meta {
  name: DELETE Unsave
  type: http
  seq: 2
}

delete {
  url: {{ base_url }}/api/saves
  body: json
  auth: inherit
  headers: {
    "Authorization": "Bearer {{ jwt }}",
    "Content-Type": "application/json"
  }
}

body:json {
  {
    "userId": "{{ user_id }}",
    "itemId": "{{ item_id }}",
    "itemType": "{{ item_type }}"
  }
}
